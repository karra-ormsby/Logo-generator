const { renderShape, renderSVG } = require('../lib/render')

// describe ('renderSVG', () => {
//     test('should take in user input and the coresponding shape (based on user input) part of and SVG file and output a complete SVG file', () => {

//     })
// })

// describe ('renderShape', () => {
//     test("give the user input of Circle should output the circle part of an SVG file", () => {
//         const expectedSVG = `<circle cx="150" cy="100" r="80" fill="${this.shapeColour}" />`;
//         const shape = new renderShape("Circle");
//         expect(shape).toEqual(expectedSVG);
//     })
// })









// describe('Circle', () => {
//     test("should take in a colour from the user, and should output the <circle> part of an SVG file", () => {
//         const shape = new Circle();
//         shape.setColor("blue");
//         expect(shape.render()).toEqual('<circle cx="150" cy="100" r="80" fill="blue" />')
//     })
// })



// describe('Header', () => {
//     test('should render header with the date', () => {
//         const expectedHtml = [
//             '<header class="header">',
//             '<h1>Todo Today</h1>',
//             `<p>${formatDate(new Date())}</p>`,
//             '</header>',
//         ].join('');
//         const header = new Header();
//         expect(header.render()).toEqual(expectedHtml);
//     });
// });
